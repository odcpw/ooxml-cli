use serde_json::Value;

use super::super::{capability_command, flag};

pub(super) fn commands() -> Vec<Value> {
    vec![
        capability_command(
            "ooxml pptx scaffold",
            "scaffold <output.pptx>",
            "Create a minimal PowerPoint presentation from scratch.",
            &["package", "slide", "shape"],
            false,
            None,
            vec![
                flag("--title", "title", "string", "initial title text"),
                flag("--subtitle", "subtitle", "string", "initial subtitle text"),
                flag(
                    "--force",
                    "force",
                    "bool",
                    "replace an existing output file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the generated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx shapes show",
            "show <file> --slide <n>",
            "Show targetable shapes on a slide.",
            &["slide", "shape"],
            false,
            Some("read-only command; generated by slides list/show"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag(
                    "--include-text",
                    "includeText",
                    "bool",
                    "include text preview/content where available",
                ),
                flag(
                    "--include-bounds",
                    "includeBounds",
                    "bool",
                    "include explicit shape bounds where available",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx shapes get",
            "get <file> --slide <n> --target <selector>",
            "Read one targetable shape on a slide.",
            &["slide", "shape"],
            false,
            Some("read-only command; generated by shapes show"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag(
                    "--target",
                    "target",
                    "string",
                    "shape selector such as title, body, shape:3, or ~Title 1",
                ),
                flag(
                    "--include-text",
                    "includeText",
                    "bool",
                    "include text preview/content where available",
                ),
                flag(
                    "--include-bounds",
                    "includeBounds",
                    "bool",
                    "include explicit shape bounds where available",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx add-textbox",
            "add-textbox <file> --slide <n> --text <text> --x <emu> --y <emu> --cx <emu> --cy <emu>",
            "Insert a text box shape on a slide at explicit EMU bounds.",
            &["slide", "shape"],
            false,
            Some("direct CLI mutation; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag("--text", "text", "string", "text box content"),
                flag("--x", "x", "int", "left position in EMUs"),
                flag("--y", "y", "int", "top position in EMUs"),
                flag("--cx", "cx", "int", "width in EMUs"),
                flag("--cy", "cy", "int", "height in EMUs"),
                flag("--name", "name", "string", "optional shape name"),
                flag("--font-size", "fontSize", "number", "font size in points"),
                flag("--font", "font", "string", "Latin font family"),
                flag("--bold", "bold", "bool", "set text bold"),
                flag("--italic", "italic", "bool", "set text italic"),
                flag("--color", "color", "string", "RGB hex text color"),
                flag("--level", "level", "int", "paragraph indent level"),
                flag("--align", "align", "string", "paragraph alignment token"),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx text set",
            "set <file>",
            "Set run-level text styling on a slide shape paragraph/run.",
            &["slide", "shape", "style", "hyperlink"],
            false,
            Some("direct CLI mutation; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag(
                    "--target",
                    "target",
                    "string",
                    "shape selector such as title, body, shape:3, or ~Shape Name",
                ),
                flag(
                    "--paragraph",
                    "paragraph",
                    "int",
                    "0-based paragraph index within the shape",
                ),
                flag(
                    "--run-index",
                    "runIndex",
                    "int",
                    "0-based text run index within the paragraph; omit to apply to all runs",
                ),
                flag("--bold", "bold", "bool", "set bold on/off"),
                flag("--italic", "italic", "bool", "set italic on/off"),
                flag(
                    "--underline",
                    "underline",
                    "string",
                    "underline kind such as sng, dbl, heavy, dotted, dash, wavy, or none",
                ),
                flag("--font-size", "fontSize", "float64", "font size in points"),
                flag("--color", "color", "string", "RGB hex color"),
                flag("--font-family", "fontFamily", "string", "latin font family"),
                flag(
                    "--hyperlink",
                    "hyperlink",
                    "string",
                    "external hyperlink URL",
                ),
                flag("--remove-bold", "removeBold", "bool", "remove bold"),
                flag("--remove-italic", "removeItalic", "bool", "remove italic"),
                flag(
                    "--remove-underline",
                    "removeUnderline",
                    "bool",
                    "remove underline",
                ),
                flag(
                    "--remove-font-size",
                    "removeFontSize",
                    "bool",
                    "remove font size",
                ),
                flag("--remove-color", "removeColor", "bool", "remove color"),
                flag(
                    "--remove-font-family",
                    "removeFontFamily",
                    "bool",
                    "remove font family",
                ),
                flag(
                    "--remove-hyperlink",
                    "removeHyperlink",
                    "bool",
                    "remove hyperlink",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx fields inspect",
            "inspect <file>",
            "Inspect PowerPoint header/footer field visibility defaults and slide placeholders.",
            &["field", "footer", "slide", "master"],
            false,
            Some("read-only command; reports existing field placeholders without creating them"),
            vec![],
        ),
        capability_command(
            "ooxml pptx fields set",
            "set <file>",
            "Update PowerPoint header/footer field visibility and existing footer/date placeholders.",
            &["field", "footer", "slide", "master"],
            false,
            Some(
                "direct CLI mutation; follows Go oracle scope and reports missing placeholders instead of creating shapes",
            ),
            vec![
                flag(
                    "--footer",
                    "footer",
                    "string",
                    "footer text for existing footer placeholders",
                ),
                flag(
                    "--show-footer",
                    "showFooter",
                    "bool",
                    "toggle master footer placeholder visibility",
                ),
                flag(
                    "--show-slide-number",
                    "showSlideNumber",
                    "bool",
                    "toggle master slide-number placeholder visibility",
                ),
                flag(
                    "--show-date",
                    "showDate",
                    "bool",
                    "toggle master date placeholder visibility",
                ),
                flag(
                    "--date-format",
                    "dateFormat",
                    "string",
                    "date field format: auto, datetime, or date-only",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx theme update",
            "update <file>",
            "Update deck theme colors and Latin major/minor fonts.",
            &["style", "slide"],
            false,
            Some(
                "direct CLI mutation; deck mode is supported, slide color mode preserves the current Go oracle error behavior",
            ),
            vec![
                flag(
                    "--color",
                    "color",
                    "string",
                    "repeatable theme color assignment name=RRGGBB",
                ),
                flag(
                    "--major-font",
                    "majorFont",
                    "string",
                    "Latin major font family",
                ),
                flag(
                    "--minor-font",
                    "minorFont",
                    "string",
                    "Latin minor font family",
                ),
                flag("--mode", "mode", "string", "theme mode: deck or slide"),
                flag(
                    "--slide",
                    "slide",
                    "int",
                    "1-based slide number for slide mode",
                ),
                flag(
                    "--for-slides",
                    "forSlides",
                    "string",
                    "slide selector list/range for slide mode",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx translate export",
            "export <file>",
            "Export PPTX slide and notes text to a translation manifest.",
            &["slide", "shape"],
            false,
            Some("read-only translation workflow; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "repeatable 1-based slide number"),
                flag(
                    "--include-notes",
                    "includeNotes",
                    "bool",
                    "include speaker notes",
                ),
                flag(
                    "--source-lang",
                    "sourceLang",
                    "string",
                    "source language tag",
                ),
                flag(
                    "--target-lang",
                    "targetLang",
                    "string",
                    "target language tag",
                ),
                flag("--format", "format", "string", "json"),
            ],
        ),
        capability_command(
            "ooxml pptx translate apply",
            "apply <file> <manifest>",
            "Apply a translation manifest to PPTX slide text.",
            &["slide", "shape"],
            false,
            Some("direct CLI translation mutation; serve/MCP op support is not wired yet"),
            vec![
                flag(
                    "--stale",
                    "stale",
                    "string",
                    "stale source handling: skip, warn, or error",
                ),
                flag("--output", "output", "string", "output file path"),
            ],
        ),
        capability_command(
            "ooxml pptx place image",
            "image <file> --slide <n> --image <path> --x <emu> --y <emu> --cx <emu> --cy <emu>",
            "Place a new image shape on a slide at explicit EMU bounds.",
            &["slide", "shape", "image"],
            false,
            Some("direct CLI mutation; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag("--image", "image", "string", "local image file path"),
                flag("--x", "x", "int", "left position in EMUs"),
                flag("--y", "y", "int", "top position in EMUs"),
                flag("--cx", "cx", "int", "width in EMUs"),
                flag("--cy", "cy", "int", "height in EMUs"),
                flag("--name", "name", "string", "optional picture shape name"),
                flag(
                    "--fit-mode",
                    "fitMode",
                    "string",
                    "fit mode: contain/fit or cover/crop",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx place table",
            "table <file> --slide <n> --data <path> --x <emu> --y <emu> --cx <emu>",
            "Place a new table shape on a slide from CSV or JSON data.",
            &["slide", "shape", "table"],
            false,
            Some("direct CLI mutation; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag("--data", "data", "string", "CSV or JSON data file path"),
                flag("--format", "format", "string", "data format: csv or json"),
                flag("--x", "x", "int", "left position in EMUs"),
                flag("--y", "y", "int", "top position in EMUs"),
                flag("--cx", "cx", "int", "width in EMUs"),
                flag(
                    "--cy",
                    "cy",
                    "int",
                    "height in EMUs; calculated from row count when omitted or zero",
                ),
                flag("--name", "name", "string", "optional table shape name"),
                flag("--header", "header", "bool", "treat first row as a header"),
                flag(
                    "--header-color",
                    "headerColor",
                    "string",
                    "header fill color as RGB hex",
                ),
                flag("--banded-rows", "bandedRows", "bool", "alternate row fills"),
                flag(
                    "--band1-color",
                    "band1Color",
                    "string",
                    "first band fill color as RGB hex",
                ),
                flag(
                    "--band2-color",
                    "band2Color",
                    "string",
                    "second band fill color as RGB hex",
                ),
                flag("--font-size", "fontSize", "int", "font size in points"),
                flag(
                    "--border-color",
                    "borderColor",
                    "string",
                    "table border color as RGB hex",
                ),
                flag(
                    "--border-width",
                    "borderWidth",
                    "int",
                    "table border width in EMUs",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx place table-from-xlsx",
            "table-from-xlsx <file> --slide <n> --workbook <xlsx> (--sheet <selector> --range <a1>|--table <selector>) --x <emu> --y <emu> --cx <emu>",
            "Place a new PowerPoint table from an XLSX range or workbook table.",
            &["slide", "shape", "table", "sheet", "range"],
            false,
            Some("direct CLI mutation; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag(
                    "--workbook",
                    "workbook",
                    "string",
                    "source XLSX workbook path",
                ),
                flag("--sheet", "sheet", "string", "source sheet selector"),
                flag("--range", "range", "string", "source A1 range"),
                flag(
                    "--table",
                    "table",
                    "string",
                    "source workbook table selector",
                ),
                flag(
                    "--max-cells",
                    "maxCells",
                    "int",
                    "maximum source cells to read; zero means unlimited",
                ),
                flag(
                    "--formula-mode",
                    "formulaMode",
                    "string",
                    "formula handling: value or formula",
                ),
                flag(
                    "--expect-source-range",
                    "expectSourceRange",
                    "string",
                    "fail unless the resolved source range matches this A1 range",
                ),
                flag("--x", "x", "int", "left position in EMUs"),
                flag("--y", "y", "int", "top position in EMUs"),
                flag("--cx", "cx", "int", "width in EMUs"),
                flag(
                    "--cy",
                    "cy",
                    "int",
                    "height in EMUs; calculated from row count when omitted or zero",
                ),
                flag("--name", "name", "string", "optional table shape name"),
                flag("--header", "header", "bool", "treat first row as a header"),
                flag(
                    "--header-color",
                    "headerColor",
                    "string",
                    "header fill color as RGB hex",
                ),
                flag("--banded-rows", "bandedRows", "bool", "alternate row fills"),
                flag(
                    "--band1-color",
                    "band1Color",
                    "string",
                    "first band fill color as RGB hex",
                ),
                flag(
                    "--band2-color",
                    "band2Color",
                    "string",
                    "second band fill color as RGB hex",
                ),
                flag("--font-size", "fontSize", "int", "font size in points"),
                flag(
                    "--border-color",
                    "borderColor",
                    "string",
                    "table border color as RGB hex",
                ),
                flag(
                    "--border-width",
                    "borderWidth",
                    "int",
                    "table border width in EMUs",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx shapes set-bounds",
            "set-bounds <file> --slide <n> --target <selector> --bounds <x,y,cx,cy>",
            "Set one PowerPoint shape's explicit bounds in EMUs.",
            &["slide", "shape"],
            false,
            Some("direct CLI mutation; serve/MCP op support is not wired yet"),
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag(
                    "--target",
                    "target",
                    "string",
                    "shape selector such as title, body, shape:3, or ~Title 1",
                ),
                flag(
                    "--bounds",
                    "bounds",
                    "string",
                    "new bounds as x,y,cx,cy EMU values",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
        capability_command(
            "ooxml pptx shapes delete",
            "delete <file> --slide <n> --target <selector>",
            "Delete one targetable PowerPoint shape from a slide.",
            &["slide", "shape"],
            true,
            None,
            vec![
                flag("--slide", "slide", "int", "1-based slide number"),
                flag(
                    "--target",
                    "target",
                    "string",
                    "shape selector such as title, body, shape:3, or ~Title 1",
                ),
                flag("--out", "out", "string", "output file path"),
                flag("--backup", "backup", "string", "backup path for --in-place"),
                flag(
                    "--dry-run",
                    "dryRun",
                    "bool",
                    "plan and validate without writing",
                ),
                flag(
                    "--in-place",
                    "inPlace",
                    "bool",
                    "write back to the input file",
                ),
                flag(
                    "--no-validate",
                    "noValidate",
                    "bool",
                    "skip strict validation of the mutated package",
                ),
            ],
        ),
    ]
}
