# Known Rust Port Contract Divergences

No intentional Rust divergences are accepted yet.

When the Rust implementation exists, every mismatch against `baseline.json`
must be classified here before it is accepted:

- `DISC-001`: pending

Accepted divergences should include impact, affected test cases, review date,
and whether the Rust behavior is intentionally different or still needs a fix.
